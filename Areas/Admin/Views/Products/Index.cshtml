@{
    ViewData["Title"] = "Product";
}

<div class="card bg-light-info shadow-none position-relative overflow-hidden">
    <div class="card-body px-4 py-3">
        <div class="row align-items-center">
            <div class="col-9">
                <h4 class="fw-semibold mb-8">Product</h4>
            </div>
            <div class="col-3">
                <div class="text-center mb-n5">
                    <img src="~/Template/dist/images/breadcrumb/ChatBc.png" alt="" class="img-fluid mb-n4" />
                </div>
            </div>
        </div>
    </div>
</div>

<div class="action-btn layout-top-spacing mb-7 d-flex align-items-center justify-content-between">
    <div class="mb-0 fs-5">
        <form class="position-relative">
            <input type="text" class="form-control search-chat py-2 ps-5" id="searchInput"
                placeholder="Search category">
            <i class="ti ti-search position-absolute top-50 start-0 translate-middle-y fs-6 text-dark ms-3"></i>
        </form>
    </div>
    <button type="button" class="btn btn-primary" onclick="window.location.href='@Url.Action("Create", "Products")'">Add
        Product</button>
</div>

<div id="product-list-container">

</div>

<div id="modal-Container">

</div>

@section Scripts {
    <script>
        const GetAllProductsUrl = '@Url.Action("ListAllProducts", "Products", new { area = "Admin" })';
        const EditProductUrl = '@Url.Action("Edit", "Products", new { area = "Admin" })';
        const DeleteProductUrl = '@Url.Action("Delete", "Products", new { area = "Admin" })';
        const DeleteProductConfirmUrl = '@Url.Action("DeleteConfirm", "Products", new { area = "Admin" })';
        $(document).ready(function () {
            GetAllProducts();
        });
    </script>
}